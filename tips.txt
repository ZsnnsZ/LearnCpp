在调试时添加打印语句，要加endl进行缓冲刷新

使用长整型字面值时用大写字母L标记

初始化不是赋值，初始化的含义是创建变量时赋予一个初始值，而赋值的含义是把对象的当前值擦除，以新值代替

初始化每一个内置类型的变量

如果在多个文件中使用同一个变量，必须将声明和定义分离

、在变量第一次使用的附近才定义它

初始化所有指针，可以初始化为nullptr或者0

面对比较复杂的引用或者指针的声明语句时，从右往左阅读

decltype((variable))的结果永远是引用，decltype(variable)只有当variable是引用时才是引用

类通常被定义在头文件中

头文件里不要用using声明

for循环里多使用!=进行判断，是因为标准库中所有容器都定义了==和!=，所以要养成使用迭代器和!=的习惯

数组是牺牲一部分灵活性来换取性能，如果不清楚元素的确切个数，要用vector

尽量使用vector 迭代器 string

